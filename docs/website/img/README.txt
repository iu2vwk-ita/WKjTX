Place the following screenshot images in this folder:

  screenshot-main.png      — WKjTX main window (hero image, wide waterfall)
  screenshot-autocall.png  — Auto-call dialog (File → Auto-call)
  screenshot-dataupdate.png — Settings → General → Data updates panel

Optional (copy from existing pages or re-use):
  favicon.ico
  icon.png                 — 512×512 PNG app icon

These files are referenced by index.html with relative paths img/screenshot-*.png
